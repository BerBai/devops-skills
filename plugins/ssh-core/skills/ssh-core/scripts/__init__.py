"""ssh-core scripts package.

Each script in this directory is an executable CLI. The package itself is
intentionally minimal — Claude Code invokes the scripts via
`python scripts/<name>.py ...`, not via `import`.
"""
